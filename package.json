{
  "name": "page-legging-test",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Web page rendering latency (lagging) diagnosis toolkit — test in Chrome, download the HAR, investigate where the time is leaking (backend / network / payload / connection / client). Zero dependencies; raw auth data never leaves the machine.",
  "scripts": {
    "analyze": "node src/analyze.mjs",
    "test": "node src/selftest.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
